<%= form_for(@project) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
      <%= f.text_area :name, placeholder: "Enter list name..." %>
    <%= f.submit "Add todo list", class: "btn btn-large btn-primary" %>
<% end %>